import 'package:flutter/material.dart';

class MoviePageButton extends StatelessWidget {
  const MoviePageButton({Key? key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
